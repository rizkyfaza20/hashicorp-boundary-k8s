apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: boundary-claim0
  name: boundary-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
